# v0.2.7 implementation tasklist

**Started:** 2026-06-11
**Target:** Ship ab0t-quota v0.2.7 with the framework described in [TICKET.md](./TICKET.md)
**Owner:** Claude
**Status:** in progress

All tasks claimed by Claude. Work proceeds top-to-bottom. Each completed task appends an entry to the WORKLOG below. **Worklog is append-only — never edit prior entries.**

---

## Tasks (delimited)

### Phase A — LedgerStore + 3 backends

- [x] `A1` Create `ab0t_quota/handler_ledger.py` skeleton: `LedgerRow` dataclass, `LedgerStatus` enum, `LedgerStore` Protocol
- [x] `A2` Implement `InMemoryLedgerStore` (simplest, used by tests)
- [x] `A3` Implement `RedisLedgerStore` (72h TTL, default for bridge mode)
- [x] `A4` Implement `DDBLedgerStore` (90d TTL, default for mesh services)
- [x] `A5` Write conformance test suite — same tests run against all 3 backends (parametrized fixture). Covers: write_attempt, read_outcome, query_by_user, query_by_status, business dedup mark/check, idempotent re-write
- [x] `A6` Wire `LedgerStore` auto-selection into `setup_quota`: prefer DDB if `app.state.ddb_client`, else Redis, else InMemory (logged loudly)

### Phase B — @idempotent decorator + HandlerContext

- [x] `B1` Implement `HandlerContext` with 4 methods: `already_done()`, `mark_done(side_effect_id)`, `skip(reason)`, `success(side_effect_id)`. Return sentinels (`SkipOutcome`, `SuccessOutcome`)
- [x] `B2` Implement `@idempotent(handler, key, retry)` decorator that marks the handler and stores config in attributes (`_ab0t_idempotent = True`, `_ab0t_idempotent_config = {...}`)
- [x] `B3` Implement in-process retry logic: exponential backoff (1s, 2s, 4s, max 30s), 3 attempts default, `retry=False` opt-out
- [x] `B4` Modify `auth_events.make_router` to detect `@idempotent` handlers and wrap them with delivery dedup + ledger + retry. Plain handlers (no `@idempotent`) keep working as today
- [x] `B5` Tests for `@idempotent`: delivery dedup blocks 2nd run, business dedup blocks 2nd run, retry on failure, max-attempts → failed_permanent, `ctx.skip`/`ctx.success` write the right rows

### Phase C — CLI subcommands

- [x] `C1` Extend `ab0t_quota/__main__.py` with `events` subcommand: query by `--user-id`, `--status`, `--handler`, `--since`. Pretty-print rows
- [x] `C2` Implement `replay` subcommand: re-fire a handler for a given `--event-id` from the stored snapshot
- [x] `C3` Implement `backfill` subcommand: fire a handler for `--user-ids u1,u2,u3` with synthetic events (no real event source)
- [x] `C4` Implement `delete-user` subcommand: GDPR cascade — delete all ledger rows for `--user-id` via GSI1 scan
- [x] `C5` CLI smoke tests: each subcommand parses args, calls the right ledger method, prints expected output (mock ledger)

### Phase D — Config wiring + consumer adoption + ship

- [x] `D1` Add `credit_grant.dedup` enum field to tier schema in `ab0t_quota/models/core.py` (`TierConfig.credit_grant: CreditGrantConfig`)
- [x] `D2` Parse `dedup` in `ab0t_quota/config.py`; default `per_user_per_tier`
- [x] `D3` Lib's auto-registered default handler reads `credit_grant.dedup`, composes the right key, uses `@idempotent` internally
- [x] `D4` Sandbox-platform: adopt `@idempotent` on `grant_credit_on_signup` (handler in `resource/output/sandbox-platform/app/quota.py`)
- [x] `D5` Write `UJ-330_handler_idempotency.sh` (was UJ-213 — renumbered to avoid collision)
- [x] `D6` Write `UJ-331_per_org_dedup_policy.sh` (was UJ-214 — renumbered to avoid collision)
- [x] `D7` Extend `docs/auth-events.md` with "Idempotency, replay, observability" section + CLI usage examples
- [x] `D8` Bump `pyproject.toml` and `__init__.py` to v0.5.2 (started from 0.5.1, not 0.2.6 — lib evolved between draft and impl)
- [x] `D9` Bump consumer pins (`resource/.../requirements.txt`, `billing/.../requirements.txt`)
- [x] `D10` Run full lib test suite — must pass (currently 312, target ≥360 with new tests)
- [ ] `D11` Run new UJs against running container — **DEFERRED to post-publish.** Requires `push.sh v0.5.2` (operator-only per memory rule). Rebuild blocked: pip install fails on `pathspec 'v0.5.2' did not match any file(s) known to git`. After publish, run `./rebuild.sh && bash UJ-330_handler_idempotency.sh && bash UJ-331_per_org_dedup_policy.sh` with `AB0T_AUTH_WEBHOOK_SECRET` exported.

---

## WORKLOG (append-only)

<!-- After completing each task, append a single block here with the format:
### YYYY-MM-DD HH:MM <task-id> — <one-line summary>
- file: <path>:<line range>
- result: <pass|fail|partial>
- notes: <anything material>
-->

### 2026-06-11 A1-A4 — handler_ledger.py module written
- file: ab0t_quota/handler_ledger.py (733 lines)
- result: pass (syntax verified)
- notes: Bundled A1-A4 into a single module write. Contains LedgerStatus enum, LedgerRow dataclass, LedgerStore Protocol, AttemptResult, InMemoryLedgerStore, RedisLedgerStore, DDBLedgerStore, HandlerContext, SkipOutcome, SuccessOutcome sentinels, @idempotent decorator, and auto_select_store. Decorator metadata attrs (_ab0t_idempotent, _ab0t_idempotent_config) read by auth_events at dispatch. Three stores share same docstring-defined Protocol contract — ready for conformance tests in A5.

### 2026-06-11 A5 — conformance test suite, 46 tests across 3 backends
- file: tests/test_handler_ledger.py (361 lines)
- result: 46/46 pass (FakeRedis + FakeDDB stubs avoid extra deps)
- notes: Parametrized fixture runs the same 11 conformance tests against all 3 LedgerStore impls — they share contract by passing the same tests. Also covers @idempotent decorator metadata, HandlerContext helpers, and auto_select_store priority (DDB > Redis > Memory with loud warning). Wrote in-process FakeRedis and FakeDDB stubs so we don't pull fakeredis/moto into test deps.

### 2026-06-11 A6+B1-B4 — LedgerStore wiring + receiver integration
- file: ab0t_quota/setup.py (~899-915), ab0t_quota/auth_events.py (+_dispatch_handler, +_run_with_retry, +_content_hash)
- result: pass (full suite 462 tests)
- notes: setup.py now calls auto_select_store and passes the result into make_router. Exposed on app.state.quota_handler_ledger for CLI/admin access. auth_events._dispatch_handler detects @idempotent via is_idempotent_handler, then delivery-deduplicates via store.record_attempt, builds HandlerContext with the configured key_fn, runs handler with exponential-backoff retry, and records final outcome. Plain (non-@idempotent) handlers run unchanged — v0.2.6 compatibility preserved. Used _content_hash fallback for events without event_id (sha256 of payload, first 32 chars).

### 2026-06-11 B5 — @idempotent integration tests through receiver
- file: tests/test_auth_events.py:TestIdempotentDispatch (5 tests appended)
- result: 5/5 pass
- notes: Tests cover delivery dedup blocking 2nd run, business-dedup via key= callable (per-org scenario with two users), retry-on-failure-then-success (3 attempts, asyncio.sleep mocked via initial_seconds=0.001), max-attempts → FAILED_PERMANENT, and the backwards-compat path where plain handlers still work. Real receiver + real ledger + real HMAC throughout — no mocks of the lib internals.

### 2026-06-11 C1-C4 — CLI subcommands (events, replay, backfill, delete-user)
- file: ab0t_quota/__main__.py (+~180 lines)
- result: pass (--help works, parses all subcommands)
- notes: Added _build_store_from_env (DDB > Redis > InMemory priority via env vars AB0T_QUOTA_DDB_TABLE / QUOTA_REDIS_URL / REDIS_URL), _parse_since (1h/24h/7d/ISO), and _print_rows (table or JSON format). `events` queries by user/status/event-id+handler. `replay` resets the ledger row to FAILED then HMAC-signs and reposts the stored event_payload to the webhook URL. `backfill` synthesizes events from user IDs (with --synthetic flag in data). `delete-user` requires --confirm (irreversible). All subcommands return shell-friendly exit codes.

### 2026-06-11 C5 — CLI smoke tests, 15 tests
- file: tests/test_cli.py (165 lines)
- result: 15/15 pass
- notes: Tests are sync because CLI uses asyncio.run internally; pytest-asyncio's running loop would conflict. Seeded InMemoryLedgerStore via direct dict injection (sync test helper _seed). Covered: events by user/status/json/empty, _parse_since hours/days/minutes/ISO, delete-user confirm guard, replay env-var guards + missing-row error, backfill required-env + required-org-id (argparse-enforced).

### 2026-06-11 D1-D3 — credit_grant.dedup schema field + composer
- file: ab0t_quota/models/core.py (added CreditGrant.dedup), ab0t_quota/auth_events.py (added compose_credit_dedup_key, updated grant_initial_credit_for_user)
- result: pass (full suite 482)
- notes: TierConfig.credit_grant gained a `dedup` string field with regex-validated enum (per_user_per_tier | per_org_per_tier | per_user_global | per_org_global). Default per_user_per_tier matches existing behaviour — no migration needed. Added compose_credit_dedup_key as a public helper exposed for consumer @idempotent handlers. grant_initial_credit_for_user now reads tier.credit_grant.dedup when present, falls back to per_user_per_tier when on the legacy initial_credits path. The redis flag key + billing idempotency_key both change shape together so they stay aligned.

### 2026-06-11 D4 — sandbox-platform adopts @idempotent
- file: resource/output/sandbox-platform/app/quota.py:191-260
- result: parse OK; lib tests still 482/482
- notes: grant_credit_on_signup now wrapped in @idempotent with handler="sandbox_grant_credit_on_signup", key=compose_credit_dedup_key("per_user_per_tier", ...), default retry policy. Signature changed to async def handler(event, ctx). All paths now return ctx.skip/ctx.success sentinels so the ledger records meaningful outcomes (missing user/org → skip with reason, engine not ready → skip, etc).

### 2026-06-11 D5-D6 — UJ-330 and UJ-331 (renumbered from 213/214)
- file: scripts/curl_tests/user_journeys/UJ-330_handler_idempotency.sh, UJ-331_per_org_dedup_policy.sh
- result: written, chmod +x; pending live run vs container (D11)
- notes: UJ-213/214 were already used for SSH features. Renumbered to 330/331 (above existing high of 329). UJ-330 fires the same event_id twice via signed POST and asserts balance doesn't double + ledger has one success row. UJ-331 fires two signups with DIFFERENT users but SAME org_id and validates the dedup behavior (skips if policy is per_org_per_tier; success-then-blocked-at-billing if per_user_per_tier). Both depend on AB0T_AUTH_WEBHOOK_SECRET env at runtime.

### 2026-06-11 D7 — docs/auth-events.md extended with framework section
- file: docs/auth-events.md (+~140 lines at end)
- result: pass (markdown valid)
- notes: New "Idempotency, replay, and observability (v0.5.2+)" section covers: @idempotent decorator with full example, the 3 layers (delivery dedup / business dedup / auto-retry / ledger), dedup policy config in quota-config.json, storage backend auto-selection table, operator CLI usage with 4 subcommands and env-var reference, migration from plain @on_auth_event (ctx-arg signature change explicit), and "when NOT to use" guidance for high-volume / strict-1-shot / auth-retry-needed cases.

### 2026-06-11 D8-D9 — version bumps
- file: pyproject.toml, ab0t_quota/__init__.py, resource/output/sandbox-platform/requirements.txt
- result: 0.5.1 → 0.5.2 (patch bump per convention)
- notes: Lib version had drifted from my ticket's v0.2.7 framing to 0.5.1 during the work between draft and impl (likely earlier features I forgot or another stream). Bumped to 0.5.2 as patch. billing/output/requirements.txt is pinned to @main, left unchanged.

### 2026-06-11 D10 — full suite final check
- file: (all tests)
- result: 482 passed in 13.44s — zero regressions
- notes: Started at 462, ended at 482 after adding +5 idempotent integration tests + +15 CLI tests. (The +46 from test_handler_ledger.py was already in 462.) No tests had to be skipped or marked xfail.

### 2026-06-11 D11 — UJ live run DEFERRED to post-publish
- file: scripts/curl_tests/user_journeys/UJ-330, UJ-331
- result: blocked — requires `bash scripts/push.sh v0.5.2` first
- notes: rebuild.sh fails with `error: pathspec 'v0.5.2' did not match any file(s) known to git` because the requirements.txt is pinned to v0.5.2 which hasn't been pushed yet. Operator runs push.sh per memory rule. After publish: cd resource/output/sandbox-platform && ./rebuild.sh && export AB0T_AUTH_WEBHOOK_SECRET=<your secret> && bash scripts/curl_tests/user_journeys/UJ-330_handler_idempotency.sh && bash scripts/curl_tests/user_journeys/UJ-331_per_org_dedup_policy.sh.
